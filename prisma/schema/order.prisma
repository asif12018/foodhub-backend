model Order {
  id         Int    @id @default(autoincrement())
  mealId     String
  mealName   String
  price      Float
  quantity   Int
  totalPrice Float
  meal       Meals  @relation(fields: [mealId], references: [id])
}
